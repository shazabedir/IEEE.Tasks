# P1 in functions:
#include <stdio.h>
long cub(long x);
int main() {
    long n, cube; 
    printf("Enter the number\n");
    scanf("%ld",&n);
    cube= cub(n);
    printf("The cube of the number is: %ld",cube);

    return 0;
}
long cub(long x) 
{
   return (x*x*x); 
}
